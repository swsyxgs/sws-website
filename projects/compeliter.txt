#include<stdio.h>
#include<iostream>
#include<bits\stdc++.h>
#include<D:\ÏîÄ¿\sws language h.h>
using namespace std; 
int a,b,sum; 
int main(int argc,char*argv[]) 
{ 

		if(argc==1){
			return 0;
		}
    	 string h;
    	 system_file_read_safe(&h,argv[1]);
    	 pd(h);
    	  
    	 
    	 
    	 cout<<endl;
    	 system("pause");
         //scanf("%d,%d",&a,&b); 
         //printf("sum=%d\n",sum); 
         
         return 0; 
} 
